---
import type { ProjectTag } from "../../../../../data";

interface Props {
  tags: ProjectTag[];
}

const { tags } = Astro.props;
---

<div class="flex flex-row flex-wrap gap-4 items-center font-code text-sm text-subtext-1">
  {tags.map((tag) => <span>#{tag}</span>)}
</div>
